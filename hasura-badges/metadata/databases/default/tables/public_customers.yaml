table:
  name: customers
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - address
        - first_name
        - last_name
      filter: {}
      query_root_fields:
        - select
      subscription_root_fields: []
