table:
  name: students
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - age
        - firstname
        - lastname
      filter: {}
      query_root_fields:
        - select
      subscription_root_fields: []
