table:
  name: requirements_definitions
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - badge_id
        - description
        - title
      filter: {}
      query_root_fields:
        - select
      subscription_root_fields: []
